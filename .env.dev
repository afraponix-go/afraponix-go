# Development Environment Configuration
NODE_ENV=development
PORT=8000

# Database Configuration (Local MariaDB via Docker)
DB_HOST=localhost
DB_USER=aquaponics
DB_PASSWORD=dev123
DB_NAME=aquaponics_dev

# JWT Configuration
JWT_SECRET=your-dev-jwt-secret-key-change-this-in-production

# SMTP Configuration (Optional for local development)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# BASE_URL=http://localhost:8000

# ThingsBoard API Configuration
THINGSBOARD_API_URL=https://tb.datascapeindustrial.com
THINGSBOARD_USERNAME=justin@aquaponics.africa
THINGSBOARD_PASSWORD=H3rcul3s!!
# THINGSBOARD_API_TOKEN will be obtained programmatically