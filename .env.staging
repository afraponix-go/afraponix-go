# Environment Configuration - STAGING
NODE_ENV=staging

# Database Configuration - Staging uses separate SQLite DB
DB_NAME=aquaponics-staging.db

# For MariaDB/MySQL staging (if preferred over SQLite)
DB_HOST=localhost
DB_PORT=3306
DB_USER=aquaponics_staging
DB_PASSWORD=staging_secure_password
# DB_NAME=aquaponics_staging

# JWT Secret (Different from production!)
JWT_SECRET=staging-jwt-secret-key-change-this-for-real-staging

# Server Configuration
PORT=3001

# CORS Origins (staging specific)
CORS_ORIGINS=http://localhost:3001,http://127.0.0.1:3001,https://staging.go.aquaponics.online,https://staging-go.aquaponics.online