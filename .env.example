# Environment Configuration
NODE_ENV=development

# Database Configuration
# For SQLite (development)
DB_NAME=aquaponics.db

# For MariaDB/MySQL (production)
DB_HOST=localhost
DB_PORT=3306
DB_USER=aquaponics
DB_PASSWORD=your_secure_password
# DB_NAME=aquaponics (same as above)

# JWT Secret (MUST change in production!)
JWT_SECRET=your-super-secure-jwt-secret-key-change-this-in-production

# Server Configuration
PORT=3000

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8000,http://127.0.0.1:8000

# ===== EMAIL SERVICE CONFIGURATION (SMTP) =====
# Required for password reset and email verification features
# If not configured, email features will gracefully degrade

# SMTP Server Configuration
SMTP_HOST=smtp.your-email-provider.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@domain.com
SMTP_PASS=your_email_password

# Email Sender Information
SMTP_FROM_NAME=Afraponix Go
SMTP_FROM_ADDRESS=noreply@your-domain.com

# Base URL for email links (production)
BASE_URL=https://your-domain.com

# ===== SECURITY CONFIGURATION =====
# Encryption Key (32-byte hex string - REQUIRED for production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_32_byte_encryption_key_in_hex

# ===== COMMON SMTP PROVIDERS EXAMPLES =====
# Uncomment and modify the section for your email provider:

# ----- Gmail -----
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-gmail@gmail.com
# SMTP_PASS=your-app-password  # Use App Password, not regular password

# ----- Outlook/Hotmail -----
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password

# ----- SendGrid -----
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key

# ===== PRODUCTION DEPLOYMENT NOTES =====
# 1. Copy this file to .env and configure with actual values
# 2. Generate secure random values for JWT_SECRET and ENCRYPTION_KEY
# 3. Configure your database connection details
# 4. Set up SMTP for email functionality (optional but recommended)
# 5. Set NODE_ENV=production for production deployment
# 6. Ensure .env file has restricted permissions (chmod 600 .env)
# 7. Never commit .env to version control